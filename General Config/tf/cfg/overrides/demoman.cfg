exec reset

fps_max 60
m_filter 1

bind E +right
bind Q +left

alias +chargejump "+jump; +attack2"
alias -chargejump "-jump; -attack2"

bind mouse3 "+chargejump"
bind mouse4 "+chargejump"
bind mouse5 "+chargejump"

